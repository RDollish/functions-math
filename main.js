const addFunction = (addition1, addition2) => addition1 + addition2;
const addingNum = addFunction(4, 3)
console.log(addingNum)

const multiFunction = (multiply) => multiply * 2;
const multiNum = multiFunction(8)
console.log(multiNum)
